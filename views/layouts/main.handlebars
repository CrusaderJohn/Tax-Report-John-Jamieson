<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tax Emporium</title>
    <link rel="stylesheet" href="/css/style.css">
    <!-- Bootstrap -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <!-- JQuery -->
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.1/jquery.min.js"></script>
</head>

<body class="bg-dark text-white">

<header class="text-center p-5">
    <div class="container">
        <h1>John's Tax Emporium</h1>
    </div>
</header>

<nav class="p-3 bg-dark">
    <div class="container">
        <div class="d-flex flex-wrap align-items-center justify-content-center justify-content-lg-start">
            <a href="/" class="d-flex align-items-center mb-2 mb-lg-0 text-white text-decoration-none">
                <svg class="bi me-2" width="40" height="32" role="img" aria-label="Bootstrap"><use xlink:href="#bootstrap"></use></svg>
            </a>

            <ul class="nav col-12 col-lg-auto me-lg-auto mb-2 justify-content-center mb-md-0">
                <li><a href="/" class="nav-link px-2 text-white">Home</a></li>
                <li><a href="/taxReportNew" class="nav-link px-2 text-white">Tax</a></li>
                {{#if logged_in}}
                <li><a href="/account" class="nav-link px-2 text-white">Account</a></li>
                {{/if}}
            </ul>

            <div class="text-end">
                <button type="button" class="btn btn-outline-light me-2" id="buttonMode">Light Mode</button>

                <!-- Conditionally render login or logout links -->
                {{#if logged_in}}
                <button type="button" class="btn btn-outline-light me-2" id="buttonLogout">Logout</button>
                {{else}}
                <button type="button" class="btn btn-outline-light me-2" id="buttonLogin">Login</button>
                {{/if}}
            </div>
        </div>
    </div>
</nav>

<main class="p-3">
    <div class="container">
        {{{ body }}}
    </div>
</main>

<footer class="p-3">
    <div class="container">

    </div>
</footer>

<!-- Render script for logged in users only -->
{{#if logged_in}}
    <script src="/js/logout.js"></script>
{{/if}}

<!-- Bootstrap -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>

<script src="/js/main.js"></script>

</body>
</html>
